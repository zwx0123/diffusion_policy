# LightDP Baseline 训练配置 - PushT任务
# 使用与剪枝模型完全相同的 LightDP 架构，不进行剪枝
# 用于公平对比：8层(不剪枝) vs 6/4/2层(剪枝后)
defaults:
  - _self_

_target_: diffusion_policy.workspace.train_diffusion_transformer_lightdp_workspace.TrainDiffusionTransformerLightDPWorkspace

# ===== 顶层变量定义 =====
obs_dim: 20
action_dim: 2
seed: 42
device: cuda:0
# =======================

name: train_diffusion_transformer_lightdp_baseline
output_dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task.name}

# 任务配置
task:
  name: pusht_lowdim
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  dataset:
    _target_: diffusion_policy.dataset.pusht_dataset.PushTLowdimDataset
    zarr_path: data/pusht/pusht_cchi_v7_replay.zarr
    horizon: ${horizon}
    pad_before: ${eval:'${n_obs_steps}-1+${n_latency_steps}'}
    pad_after: ${eval:'${n_action_steps}-1'}
    obs_key: keypoint
    state_key: state
    action_key: action
    seed: ${seed}
    val_ratio: 0.02
    max_train_episodes: 90
  env_runner:
    _target_: diffusion_policy.env_runner.pusht_keypoints_runner.PushTKeypointsRunner
    keypoint_visible_rate: ${keypoint_visible_rate}
    n_train: 6
    n_train_vis: 2
    train_start_seed: 0
    n_test: 50
    n_test_vis: 4
    legacy_test: True
    test_start_seed: 100000
    max_steps: 300
    n_obs_steps: ${n_obs_steps}
    n_action_steps: ${n_action_steps}
    n_latency_steps: ${n_latency_steps}
    fps: 10
    agent_keypoints: False
    past_action: ${past_action_visible}
    n_envs: 8

# Policy配置 - 使用与剪枝模型完全相同的 LightDP 架构
policy:
  _target_: diffusion_policy.policy.diffusion_transformer_lightdp_policy.DiffusionTransformerLightDPPolicy
  
  model:
    _target_: diffusion_policy.model.diffusion.transformer_for_diffusion_lightdp.LightDPTransformerForDiffusion
    input_dim: ${eval:'${action_dim} if ${obs_as_cond} else ${obs_dim} + ${action_dim}'}
    output_dim: ${eval:'${action_dim} if ${obs_as_cond} else ${obs_dim} + ${action_dim}'}
    horizon: ${horizon}
    n_obs_steps: ${n_obs_steps}
    cond_dim: ${eval:'${obs_dim} if ${obs_as_cond} else 0'}

    n_layer: 8
    n_head: 4
    n_emb: 256
    p_drop_emb: 0.0
    p_drop_attn: 0.01

    causal_attn: True
    time_as_cond: True
    obs_as_cond: ${obs_as_cond}
    n_cond_layers: 0
    
    # LightDP 剪枝参数 - baseline 不剪枝，target_layers=n_layer
    init_gate_score: 0.5
    target_layers: 8
    warmup_epochs: ${lightdp.warmup_epochs}
    svd_rank: ${lightdp.svd_rank}
  
  noise_scheduler:
    _target_: diffusers.schedulers.scheduling_ddpm.DDPMScheduler
    num_train_timesteps: 100
    beta_start: 0.0001
    beta_end: 0.02
    beta_schedule: squaredcos_cap_v2
    variance_type: fixed_small
    clip_sample: True
    prediction_type: epsilon

  horizon: ${horizon}
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  n_action_steps: ${n_action_steps}
  n_obs_steps: ${n_obs_steps}
  num_inference_steps: 100
  obs_as_cond: ${obs_as_cond}
  pred_action_steps_only: ${pred_action_steps_only}

# LightDP配置 - baseline 不剪枝
# 训练流程: warmup(30 epochs) -> pruning(0 epochs) -> finetune(230 epochs)
# 总 epoch = 260，与剪枝模型完全一致
lightdp:
  warmup_epochs: 30
  pruning_epochs: 0
  target_layers: 8
  finetuning_epochs: 230
  svd_rank: 4

# 模型参数
horizon: 16
n_obs_steps: 2
n_action_steps: 8
n_latency_steps: 0
past_action_visible: False
keypoint_visible_rate: 1.0
obs_as_cond: True
pred_action_steps_only: False

# EMA配置
ema:
  _target_: diffusion_policy.model.diffusion.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

# 数据加载器
dataloader:
  batch_size: 256
  num_workers: 0
  shuffle: True
  pin_memory: False
  persistent_workers: False

val_dataloader:
  batch_size: 256
  num_workers: 0
  shuffle: False
  pin_memory: False
  persistent_workers: False

# 优化器 - 与剪枝模型完全一致
optimizer:
  learning_rate: 1.0e-4
  weight_decay: 0.1
  betas: [0.9, 0.95]

# 训练配置 - 与剪枝模型完全一致
training:
  device: "cuda:0"
  seed: 42
  debug: False
  resume: False
  lr_scheduler: cosine
  lr_warmup_steps: 50
  num_epochs: 260
  gradient_accumulate_every: 1
  use_ema: True
  rollout_every: 30
  checkpoint_every: 30
  val_every: 1
  sample_every: 10
  max_train_steps: null
  max_val_steps: null
  tqdm_interval_sec: 1.0
  grad_clip_norm: 1.0

# 日志配置
logging:
  project: lightdp
  resume: True
  mode: disabled
  name: ${now:%Y.%m.%d-%H:%M.%S}_${name}_${task.name}
  tags: ["${name}", "${task.name}", "${exp_name}"]
  id: null
  group: null

exp_name: "lightdp_baseline"
task_name: ${task.name}

# 检查点
checkpoint:
  topk:
    monitor_key: test_mean_score
    mode: max
    k: 5
    format_str: 'epoch={epoch:04d}-test_mean_score={test_mean_score:.3f}.ckpt'
  save_last_ckpt: True
  save_last_snapshot: False

multi_run:
  run_dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task.name}
  wandb_name_base: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task.name}

hydra:
  job:
    override_dirname: ${name}
  run:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task.name}
  sweep:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task.name}
    subdir: ${hydra.job.num}